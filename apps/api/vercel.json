{
  "installCommand": "npm install prisma@5.22.0 @prisma/client@5.22.0 @vercel/functions fastify @fastify/cors && npx prisma generate",
  "buildCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
