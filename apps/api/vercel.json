{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter=@axis/db generate && pnpm --filter=@axis/api build",
  "outputDirectory": "dist",
  "functions": {
    "dist/index.js": {
      "runtime": "nodejs20.x",
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ]
}
