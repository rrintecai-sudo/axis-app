{
  "installCommand": "npm install prisma @prisma/client @vercel/functions fastify @fastify/cors && npx prisma generate",
  "buildCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
